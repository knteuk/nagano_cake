<div class="container my-5">
  <div class="row">
    <!--<%#= render 'public/items/genre_search', genres: @genres %>-->
    <div class="cover-img mx-auto rounded">
      <div class="d-flex align-items-center justify-content-center h-100">
        <p style="color: #fff;">
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトです。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
	  </div>
	</div>

    <h3 class="red-border px-3 pb-1">新着商品</h3>
    <div class="row mx-auto mt-3">
      <% @items.last(4).each do |item| %>
        <div class="mx-auto">
          <%= link_to item_path(item) do %>
            <%= attachment_image_tag item, :image, size: '150x150', fallback: 'no_image.png', class: "rounded" %><br>
          <% end %>
          <div class="row mt-3">
            <strong><span class="red-border px-3"><%= item.name %></span></strong>
          </div>
          <div class="row mt-1">
            <span class="offset-8"><%= (item.price * 1.1).to_i.to_s(:delimited) %> 円</span>
          </div>
        </div>
      <% end %>
      <div class="row mt-3">
        <%= link_to "全ての商品を見る >", items_path %>
      </div>
    </div>
</div>